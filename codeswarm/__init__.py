"""CodeSwarm — a provider-agnostic swarm of LLM agents that build software.

The swarm runs the full software lifecycle (requirements -> design -> planning
-> per-feature build/test/review -> integration) and gates each feature behind
a real quality check: its tests must pass and a reviewer agent must approve
before the feature is considered done.
"""

__version__ = "0.1.0"
